apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: dev-kafka
  namespace: argocd
spec:
  project: leninkart
  source:
    repoURL: https://charts.bitnami.com/bitnami
    chart: kafka
    targetRevision: 26.9.0   # <-- matches what Argo CD is using
    helm:
      values: |
        kraft:
          enabled: false

        controller:
          replicaCount: 0     # 👈 CRITICAL FIX

        broker:
          replicaCount: 1     # 👈 CRITICAL FIX
          persistence:
            enabled: false

        zookeeper:
          enabled: true
          replicaCount: 1
          persistence:
            enabled: false

        persistence:
          enabled: false
  destination:
    server: https://kubernetes.default.svc
    namespace: dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true