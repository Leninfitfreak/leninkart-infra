route:
  receiver: ai-observer-webhook
  routes:
    - receiver: ai-observer-webhook
      continue: true

receivers:
  - name: ai-observer-webhook
    webhook_configs:
      - url: http://ai-observer.dev.svc.cluster.local:8080/webhook/alertmanager
        send_resolved: true
