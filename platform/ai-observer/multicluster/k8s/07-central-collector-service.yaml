apiVersion: v1
kind: Service
metadata:
  name: ai-observer-central-collector
  namespace: dev
spec:
  selector:
    app: ai-observer-central-collector
  ports:
    - name: http
      protocol: TCP
      port: 8081
      targetPort: http