apiVersion: v1
kind: Service
metadata:
  name: leninkart-frontend
  labels:
    app: frontend
spec:
  type: {{ .Values.service.type | default "ClusterIP" }}
  selector:
    app: frontend
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
